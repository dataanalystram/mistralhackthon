---\nname: lint-fix\ndescription: Automatically fixes linting issues in all project files using ESLint's --fix flag.\nuser-invocable: true\nallowed-tools:\n  - bash\n  - read_file\n  - write_file\n  - grep\n  - git\n---\n# /lint-fix\n## Description\nAutomatically fixes linting issues in all project files using ESLint's --fix flag.\n## Steps\n1. **Check if ESLint is available** — `command -v eslint`\n2. **Run ESLint in diagnostic mode** — `eslint . --quiet`\n3. **Apply ESLint auto-fixes** — `eslint . --fix`\n4. **Summarize linting results** — `eslint . --quiet || echo 'Linting complete with remaining issues'`\n## Safety\n- Ensure ESLint is available before proceeding\n- Confirm before applying fixes\n- Handle errors gracefully\n- Respect allowed paths and deny globs